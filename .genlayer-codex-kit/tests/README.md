# Regression fixtures
Cover no contract, copied example, missing source, deterministic-only, shape-only validator, validator re-execution, valid validator, ambiguity, simulated wallet, static frontend, wrong address, mocked transaction, zero transfer, unsafe URL, silent fallback, missing access control, Studio unavailable, Explorer unavailable, and valid end-to-end.
